radius(num){
  -webkit-border-radius: num;
     -moz-border-radius: num;
          border-radius: num;
}
blur-box(offsetX, offsetY, blurRadius, spreadRadius, color){
  -webkit-box-shadow: offsetX offsetY blurRadius spreadRadius color;
          box-shadow: offsetX offsetY blurRadius spreadRadius color;
}

body
  background-color: #454545
  font-family: sans-serif
  font-weight: 100
  text-align: center
[class$='-cl']
  background-color: #434343
  color: #86868C
  radius(25px)
  border: 4px solid rgba(248,248,255,0.67)
  blur-box(0, 0, 20px, 0, ghostwhite)
.demo-title-container
  width: 100%
  max-width: 1440px
  margin: 0 auto
.demo-generic-container
  width: 90%
  max-width: 1240px
  margin: 1% auto
  radius(20px)
  .rw
    margin: 20px 0 20px 0
  // blur-box(0, 0, 20px, 0, #00ffff)
.demo-hdln
  text-transform: uppercase
  font-weight: 100
  color: #313131
  letter-spacing: 0.4em
.demo-title
  margin-bottom: 40px
  radius(60px)
  blur-box(0, 0, 20px, 0, #00ffff)
  border: 0
  background-color: #272727
.demo-text
  font-size: 0.8em
